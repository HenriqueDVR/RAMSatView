requests>=2.31
PyYAML>=6.0
pytest>=8.0

# Reading the NOAA satellite mosaic, which ships as netCDF4 (i.e. HDF5).
# h5py below 3.10 is built against numpy 1 and dies on numpy 2 with
# "numpy.dtype size changed", so the floor is not cosmetic.
h5py>=3.10
numpy>=1.26
